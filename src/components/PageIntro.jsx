import { motion } from 'framer-motion'
import { maskReveal, revealUp } from '../utils/motion'
export default function PageIntro({ eyebrow, title, description }) { return <motion.section initial="hidden" animate="visible" exit={{ opacity: 0 }} className="mx-auto max-w-[1440px] px-6 py-24 md:px-10 md:py-32 lg:px-14"><motion.p variants={revealUp} className="mb-6 text-[10px] font-bold uppercase tracking-[0.22em] text-signal">{eyebrow}</motion.p><div className="overflow-hidden"><motion.h1 variants={maskReveal} className="max-w-5xl text-5xl font-semibold leading-[.9] tracking-[-0.075em] md:text-7xl lg:text-8xl">{title}</motion.h1></div>{description && <motion.p variants={revealUp} transition={{ delay: 0.16 }} className="mt-8 max-w-2xl text-base leading-relaxed text-ink/70 md:text-lg">{description}</motion.p>}</motion.section> }
